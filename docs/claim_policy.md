# Claim policy
- Do not claim universal speedup over `std::cos`.
- Do not compare reduced-input functions against general `std::cos` without explaining input contracts.
- Always report platform, compiler, flags, range, and sample count.
- Keep negative results.
