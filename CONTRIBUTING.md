# Contributing
Run `cmake -S . -B build`, build, and run `ctest --test-dir build -C Release`.
Run `toltrig_bench --quick` before submitting benchmark changes.
Do not submit speed claims without CSV output and platform, compiler, flags, and input-range metadata.
